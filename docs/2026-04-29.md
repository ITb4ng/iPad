# 2026-04-29 작업 방향

## 작업 브랜치
`feat/portfolio-polish`

## 대상 프로젝트
`iPad 11` 프로젝트

## 작업 목적
이번 작업은 `iPad 11` 프로젝트를 단순한 UI 클론 결과물에서, 포트폴리오용 리뉴얼 프로젝트로 다시 정리하기 위한 작업이다.

이 프로젝트는 처음부터 실무형 완성 프로젝트로 시작한 것이 아니라, 공홈 기반의 정적 학습용 UI 구현 프로젝트에서 출발했다.  
따라서 이번 브랜치의 목적은 Apple 공홈을 그대로 복제하는 것이 아니라, 기존 프로젝트의 한계를 분석하고 현재 시점에서 설명 가능한 구조로 재정리하는 데 있다.

이번 작업에서는 아래 내용을 문서와 코드에 함께 반영한다.

- 기존 학습형 프로젝트의 문제점
- Apple 공홈과 현재 프로젝트의 차이
- 현재 리뉴얼 과정에서 유지할 부분과 덜어낼 부분
- iPad 구매를 고려하는 사용자에게 어떤 흐름으로 정보를 전달할지
- 접근성, 마크업 역할, 반응형 기준을 왜 지금 구조로 선택했는지

즉 이 브랜치는 단순히 화면을 더 비슷하게 만드는 작업이 아니라,  
기존 클론 프로젝트를 현재 수준에서 설명 가능한 포트폴리오 프로젝트로 바꾸는 정리 작업이다.

---

## 프로젝트 리뉴얼 방향

### 기존 프로젝트의 출발점
기존 `iPad 11` 프로젝트는 Apple 공홈을 기반으로 한 UI 구현 학습용 프로젝트였다.

HTML, CSS, JavaScript로 구조와 시각 효과, 일부 인터랙션을 구현하는 데 초점이 있었고,  
전체 구조도 공홈을 참고한 시각 재현 중심 성격이 강했다.

### 기존 프로젝트의 문제점
- 단순 UI 구현 중심이라 프로젝트의 개선 의도가 문서로 정리되어 있지 않다.
- 헤더 이후 섹션의 콘텐츠 목적과 사용자 흐름이 충분히 설명되지 않는다.
- 실제 iPad 구매를 고려하는 사용자가 어떤 정보를 어떤 순서로 보게 되는지 설계 근거가 부족하다.
- 기존 마크업이 공홈 링크를 많이 포함하고 있어 프로젝트 자체의 맥락이 약해 보일 수 있다.
- 링크와 버튼의 역할이 혼재되면 접근성과 탐색 흐름이 어색해질 수 있다.
- 결과적으로 단순 클론처럼 보일 위험이 있다.

### Apple 공홈과의 차이 정리
Apple 공홈은 시각 완성도와 인터랙션 밀도가 매우 높은 브랜드 랜딩 페이지다.

하지만 이 프로젝트는 공홈을 그대로 재현하는 것이 목적이 아니라,

- 공홈의 시각 흐름과 정보 구조는 참고하되
- 현재 프로젝트 수준에서 유지보수 가능한 방식으로 정리하고
- 사용자 탐색 흐름과 접근성을 더 설명 가능한 구조로 바꾸는 것

에 목적이 있다.

특히 스크롤 기반 헤더 접근성, 모바일 패널 포커스 제어, 링크/버튼 정책 정리는  
단순 시각 재현보다 실제 사용성과 설명 가능성을 높이기 위한 리뉴얼 포인트로 본다.

### 리뉴얼 프로젝트의 방향
이번 리뉴얼은 “원본처럼 보이는 결과물”보다 “왜 이렇게 고쳤는지 설명할 수 있는 결과물”을 목표로 한다.

- 의미 있는 마크업
- 링크와 버튼 역할 구분
- 키보드 탐색 흐름
- 모바일 패널 포커스 제어
- 접근성 상태 관리
- 반응형 인터랙션 기준
- 불필요한 구조 제거

이 기준을 중심으로 포트폴리오용 품질로 정리한다.

---

## 전체 작업 순서

1. 프로젝트 설명을 포트폴리오용 컨텍스트로 정리
2. 마크업 정책 반영
3. 리소스 최신화
4. 반응형 디테일 점검
5. 브라우저 테스트

---

## 1. 프로젝트 설명을 포트폴리오용 컨텍스트로 정리

### 작업 목적
현재 프로젝트 설명을 단순 구현 기록이 아니라, 기존 학습 프로젝트를 분석하고 개선한 포트폴리오용 리뉴얼 프로젝트처럼 보이도록 정리한다.

### 작업 내용
- README 및 관련 문서의 소개 문구 정리
- Apple 공홈 UI 클론처럼 보이는 인상 줄이기
- `iPad` 상품 소개 페이지를 모티브로 한 인터랙션 리뉴얼 프로젝트로 방향 정리
- 기존 프로젝트의 문제점 작성
- 공홈과 현재 프로젝트의 차이 작성
- 스크롤 기반 헤더 개선 이유 작성
- 공홈 링크를 왜 줄이거나 재정리하는지 설명
- 실제 사용자 흐름을 기준으로 콘텐츠 목적 정리

### 확인 기준
- 프로젝트가 단순 클론처럼 보이지 않는다.
- 기존 프로젝트의 한계와 개선 의도가 설명된다.
- 공홈과의 차이와 참고 범위가 정리된다.
- 현재 수준에서 유지보수 가능한 방향이 드러난다.

### 예상 커밋
```bash
docs: 포트폴리오용 프로젝트 설명 정리
```

---

## 2. 마크업 정책 반영

### 작업 목적
프로젝트 안에서 링크와 버튼의 역할을 명확히 구분하고, 접근성과 탐색 흐름 기준에 맞는 마크업으로 정리한다.

### 연결 문서
- [링크 / 버튼 마크업 정책](./link-button-policy.md)

### 우선 적용 범위
1. 헤더
2. 제품 내비게이션
3. 본문 섹션 CTA 및 인터랙션 요소
4. 푸터 링크 및 기존 링크 구조

### 작업 내용
- 실제 페이지 이동이 필요한 경우에만 `<a>` 사용
- 현재 페이지 안의 상태를 제어하는 경우 `<button>` 사용
- `href="#"`, `javascript:void(0)`, 클릭 가능한 `div` / `span` 패턴 제거
- 헤더와 푸터에 남아 있는 불필요한 외부 이동 링크 정리 또는 역할 재검토
- 현재 페이지 컨텍스트와 맞지 않는 링크는 유지 여부를 다시 판단
- 본문 CTA를 `내부 이동`, `외부 정보 이동`, `구매/전환 CTA`로 분류
- 실제 구현되지 않은 기능은 억지로 링크로 만들지 않고 계획 상태로 구분
- 링크와 버튼 역할이 키보드/스크린리더 기준으로도 일관되게 읽히도록 정리

### 사용자 흐름 기준
- 헤더는 주요 탐색 흐름을 빠르게 제공한다.
- 본문 CTA는 정보 탐색 흐름에 맞는 순서로 배치한다.
- 푸터는 정책/보조 링크 중심으로 정리한다.
- 장식 요소는 탐색 흐름에서 제외한다.

### 확인 기준
- 링크와 버튼의 역할이 명확하다.
- 의미 없는 가짜 링크가 없다.
- 헤더, 본문, 푸터의 탐색 흐름이 자연스럽다.
- 스크린리더가 링크와 버튼 역할을 올바르게 읽는다.
- 정책 문서와 실제 구현 사이의 괴리가 크지 않다.

### 현재 반영 상태
- 헤더, 제품 내비게이션, 푸터 기준의 링크 / 버튼 역할 구분은 반영 완료
- 본문 CTA는 `내부 이동`, `외부 정보`, `구매/전환 CTA` 기준으로 분류 가능한 상태까지 정리 완료
- 공홈 외부 링크는 프로젝트 내부 경로로 재정리 완료
  - 예: `/buy`, `/trade-in`, `/learn/...`, `/legal/...`
- 비교 카드, 푸터 내비게이션 같은 동적 링크도 내부 경로 기준으로 정리 완료
- `404.html`을 추가해 내부 경로가 없는 경우 보여줄 콘텐츠도 준비 완료
- 단, `/buy` 같은 경로가 실제로 `404.html`로 연결되려면 로컬 서버 또는 배포 환경에서 custom 404 fallback 설정이 필요함

### 마감 판단
이 항목은 **마크업 정책 반영 관점에서는 마감 가능** 상태로 본다.  
남은 작업은 태그 역할 정리보다 서버/배포 라우팅 설정에 가깝다.

### 예상 커밋
```bash
refactor: 마크업 정책 기준으로 링크와 버튼 역할 정리
```

---

## 3. 리소스 최신화

### 작업 목적
프로젝트 안에서 사용하는 이미지, 아이콘, OG 리소스, 문서 참조 자산을 다시 확인하고 정리한다.

### 작업 내용
- 사용하지 않는 이미지/아이콘/기타 리소스 확인
- 문서에서 참조하는 리소스 경로 점검
- hero 및 주요 섹션 비주얼 자산 점검
- alt 기준 재확인
- 현재 리뉴얼 컨텍스트와 맞지 않는 리소스 정리

### 확인 기준
- 끊긴 리소스 경로가 없다.
- 주요 이미지와 아이콘이 정상 표시된다.
- 정보 이미지의 대체 텍스트가 목적에 맞게 정리된다.

### 예상 커밋
```bash
chore: 프로젝트 리소스 경로 및 자산 정리
```

---

## 4. 반응형 디테일 점검

### 작업 목적
모바일, 태블릿, 데스크탑 주요 구간에서 레이아웃과 인터랙션 디테일을 다시 확인한다.

### 작업 내용
- hero / sticky / scroll interaction 구간 점검
- 스크롤 방향 기반 헤더 노출 흐름 점검
- 작은 화면에서 버튼 간격, 겹침, 터치 영역 점검
- 본문 CTA 흐름 점검
- hover / focus / active 상태 점검
- reduced motion 환경 점검
- 가로 스크롤 발생 여부 점검

### 확인 기준
- 주요 구간에서 레이아웃이 깨지지 않는다.
- 헤더와 패널 인터랙션이 일관되게 동작한다.
- 키보드 및 포인터 사용 모두에서 탐색 흐름이 자연스럽다.

### 예상 커밋
```bash
style: 반응형 디테일 및 인터랙션 마감 정리
```

---

## 5. 브라우저 테스트

### 작업 목적
로컬 및 배포 기준으로 주요 브라우저에서 핵심 흐름을 확인하고 기록한다.

### 작업 내용
- Chrome 데스크탑 점검
- Edge 데스크탑 점검
- iPhone Safari 점검
- iPhone Chrome 점검
- 키보드 탐색 흐름 점검
- 헤더/패널/푸터 인터랙션 점검
- 이미지 및 리소스 표시 상태 점검
- 발견 이슈 기록

### 확인 기준
- 주요 브라우저에서 핵심 흐름이 정상 동작한다.
- 키보드 탐색과 포커스 흐름이 유지된다.
- 리소스가 정상 로드된다.
- 남은 이슈가 있으면 문서화된다.

### 예상 커밋
```bash
test: 주요 브라우저 점검 내용 정리
```

---

## 오늘 바로 진행할 항목

오늘은 `2. 마크업 정책 반영`을 우선 진행한다.

특히 헤더는 사용자가 어느 위치에서든 다시 주요 탐색 흐름으로 돌아갈 수 있게 만드는 핵심 영역이므로,  
포트폴리오형 리뉴얼의 기준과 직접 연결된다.

### 오늘 작업 기준
- 링크 정책 문서를 기준으로 현재 프로젝트 헤더 구조 점검
- 헤더 내부 이동 요소와 제어 요소의 역할 구분
- 의미 없는 링크 또는 미사용 요소 정리
- 헤더 이후 제품 내비게이션과 푸터까지 같은 기준 확장
- 변경 범위와 남은 작업 분리

### 오늘 예상 커밋
```bash
refactor: 마크업 정책 기준으로 헤더와 주요 링크 구조 정리
```

---

## 이번 브랜치의 최종 목표

이번 브랜치가 끝났을 때 `iPad 11` 프로젝트는 아래 상태에 가까워져야 한다.

- 단순 공홈 클론이 아니라 포트폴리오용 리뉴얼 프로젝트로 설명된다.
- 기존 프로젝트의 문제점과 개선 이유가 문서로 정리된다.
- 공홈과의 차이와 참고 범위가 설명된다.
- 링크와 버튼의 역할이 명확하게 구분된다.
- 헤더, 본문, 푸터의 탐색 흐름이 사용자 기준으로 정리된다.
- 접근성과 반응형 인터랙션 기준이 현재 코드에 반영된다.
- 리소스와 문서 참조가 안정적으로 유지된다.

결론적으로 이번 작업은 기존 학습 프로젝트를 분석하고,  
현재 시점에서 이해 가능하고 설명 가능한 포트폴리오형 리뉴얼 프로젝트로 다시 정리하는 과정이다.
